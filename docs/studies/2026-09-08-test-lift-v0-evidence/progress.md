# SDD ledger — plan: docs/studies/2026-09-08-test-lift-v0-plan.md
Spec: ~/Codes/daily-logs/researches/property-belief-manipulation/designs/2026-09-08-graded-commitment-design.md §11
Branch: study/test-lift-belief-rerank (main checkout of ~/Codes/RoboLab, base 9db0aaf)

## Preflight scan (2026-09-08)
| pair / task | produces vs consumes | finding |
|---|---|---|
| T1↔T2 | skew, GRAVITY_G | consistent |
| T1↔T3 | p_hold, GRAVITY_G; T3 re-derives margin vectorised | duplication of the margin formula — see Ruling 2 |
| T2↔T8 | update_from_wrench(b,f_o,tau_o,p_hand_o,g_hat_o,R_f,R_tau) | consistent |
| T3↔T5 | GraspParams.depth / FRANKA_PANDA_DEPTH | interfaces text says FRANKA_PANDA_DEPTH, code shows literal — see Ruling 3 |
| T4↔T8 | all frames.py functions; run_grasp 5-tuple unpack | consistent |
| T5↔T8 | GraspGenClient.infer; write_episode requires all 24 EPISODE_KEYS; T8 log carries all 24 | consistent |
| T6↔T7 | make_object_physics_events_cfg_xyz; events_cfg passed as callable | plan flags callable-vs-instance as to-verify at T6 |
| T7↔T8 | register_test_lift_env positional signature | consistent |
| T8↔T9 | dir layout <out>/<obj>/off_XXcm/<arm>/seed_k.npz; results parses off[4:6] | consistent |
| T1..T5,T9 self | test expectations recomputed by hand (margin 0.32, hull mass ~0.7, tight-belief argmax at x=0.06, frame rotations) | consistent |
| T6 self | test reads private attr _robolab_authored_coms | fragile but acceptable; no ruling needed |

Ruling 1: work in the main checkout on branch study/test-lift-belief-rerank, not a worktree — user asked for a checkout, and a worktree needs a fresh ~19 GB Isaac venv. Cost if wrong: untracked files from main are visible; none are touched.
Ruling 2: T3 may re-implement the margin vectorised in _hold_prob_matrix; implementer must add one test asserting _hold_prob_matrix equals physics.margin/p_hold for a single sample. Cost if wrong: two formulas drift silently.
Ruling 3: T3 defines module constant FRANKA_PANDA_DEPTH = 0.10527314 and GraspParams.depth defaults to it; T5 Step 0 overwrites the value. Cost if wrong: none beyond T5's edit.
Ruling 4: no TodoWrite tool in this session — this ledger is the task tracker.

## Tasks
Task 1: review — 1 Important (conftest docstring stale), minors: analysis/__init__.py added (necessary, ledgered), SPDX headers missing on new files, kappa/alpha untested. Trailers verified by controller (2/2).
Ruling 5: SPDX header (`# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.` / `# SPDX-License-Identifier: Apache-2.0`) is required on every new .py file for the rest of this plan; fix in Task 1 round 1 alongside the Important finding. Cost if wrong: none.
Task 1: minor (deferred): no test for margin kappa/alpha away from defaults.
Task 1: fix round 1/5 (2 addressed, 0 open; commits 1245f45..3c844bb)
Task 1: complete (commits f620e3c..3c844bb, review clean)
Task 2: review — 1 Important (plan-mandated): variance along g shrinks for anisotropic c_cov with non-axis-aligned g.
Ruling 6: NOT a defect — parked. H g = 0 so the measurement carries no direct information about c∥, but P⁺ = P − PHᵀS⁻¹HP is exact Bayes: when g is not an eigenvector of P the prior correlates c∥ with c⊥ in the g-aligned basis, and that correlation legitimately transfers information. The invariant "variance along g unchanged" holds iff g is an eigenvector of c_cov; the spec §11.2 wording is loose and will be corrected in the design doc. Cost if wrong: none for correctness; only the wording of the reported "unidentified component" changes.
Task 2: minor (deferred): sample() mass clip 0.05*m_mean with a misleading comment; no guard for degenerate ConvexHull input; missing docstrings on prior_from_points/update_mass/update_from_wrench.
Task 2: complete (commits 3c844bb..8fed72c, 1 parked)
Task 3: implementer DONE_WITH_CONCERNS — test_wide_belief_falls_back_toward_geometry fails (idx 3 beats 4 by ~0.008). Cause: Monte-Carlo noise — with a wide belief the mean hold probability is ~1%, so 256 samples give ~2-3 hits per candidate.
Ruling 7: plan defect in the test, not the code. Raise GraspParams.n_samples default 256 -> 2048 (vectorised, negligible cost; the same noise would hit the real first-grasp choice under the wide prior b0), and have the wide-belief test use n_samples=16384 so the MC error is far below the 0.054 confidence gap. Cost if wrong: ~8x compute in the scorer, still milliseconds.
Task 3: fix round 0 (pre-review, Ruling 7 applied; commits 5610e2d..17d8d9d)
Task 3: minor (deferred): FRANKA_PANDA_DEPTH lacks a "provisional, Task 5 overwrites" comment; 1e-6 clip in score_candidates undocumented/untested.
Task 3: complete (commits 8fed72c..17d8d9d, review clean)
Task 4: minor (deferred): HAND_YAW_FIX arrays are mutable module state (no copy on lookup); no standalone subtract_bias test; bare KeyError on bad yaw_fix.
Task 4: complete (commits 17d8d9d..d1588cf, review clean)
Ruling 8 (Task 5/8): GraspGenX pins torch>=2.1,<2.7 (cu124) for the authors' 560.x driver. The local GPU is an RTX 5090 (sm_120, driver 580.173.02); cu124 torch has no sm_120 kernels, so GraspGenX inference would fail at runtime. After `uv sync`, override in the GraspGenX venv: `uv pip install --python .venv/bin/python "torch==2.7.0" "torchvision==0.22.0" --index-url https://download.pytorch.org/whl/cu128`. Consequence: never use `uv run` in ~/Codes/GraspGenX afterwards (it re-syncs and reverts torch) — always call `.venv/bin/python` directly, including the Task 8 server launch. Cost if wrong: the override breaks a compiled dep (pointnet2_ops / torch-geometric); fallback is to serve GraspGenX from the RoboLab venv (torch 2.7.0+cu128) via `uv pip install -e ~/Codes/GraspGenX --no-deps` plus its pure-python deps.
Ruling 9 (Task 5, supersedes the "uv sync then override" order of Ruling 8): `uv sync` in GraspGenX had run 22 min at ~0.6 MB/s, still downloading the cu124 torch stack that Ruling 8 discards anyway. Kill it (exact PID) and build the venv manually: `uv venv --python 3.11 .venv`, install torch 2.7.0+cu128 (already in the uv cache from RoboLab), then `uv pip install -e . --no-deps` plus the pyproject dependency list minus torch/torchvision, plus the `serve` extra. Same "never `uv run` in GraspGenX" rule. Cost if wrong: a transitive dep version differs from uv.lock; pure-python deps, low risk.
Task 5: depth = 0.1034 m from graspgenx.x_grippers.resolve_gripper_info("franka_panda") (brief's graspgenx.robot.get_gripper_depth is a dead path). GraspGenX venv: manual, torch 2.7.0+cu128, ext/ cloned. NEVER `uv run` in ~/Codes/GraspGenX.
Task 5: minor (deferred): available() catches bare Exception; validate_episode checks keys only, not shapes.
Task 5: complete (commits d1588cf..c63f249, review clean)
Task 6: implementer found three branch facts that change Tasks 7-8: (a) `auto_discover_and_create_cfgs` on this branch has no `events_cfg` kwarg — physics events go to `create_env(name, ..., events=cfg)` (merged via merge_events_cfg); (b) `contact_gripper` + a scene without a `table` entity crashes create_contact_sensors; (c) `tasks=` takes a bare filename searched under task_dir, not "subdir/file.py".
Ruling 10 (Tasks 7-8): `register_test_lift_env(task_file, object_name, mass_kg, com_offset_xyz, postfix) -> tuple[str, ObjectPhysicsEventsCfg]`; callers do `env, _ = create_env(name, device=..., num_envs=1, use_fabric=True, events=events)`. Register with `contact_gripper=None` and `contact_object_list` limited to entities the scene actually defines (the driver judges outcomes from object height and finger gap, not contact sensors). Pass `tasks="banana_test_lift_task.py"` (bare filename). Cost if wrong: no contact sensors in v0 — none are needed.
Task 6: review — 1 Important (plan-mandated): xyz builder writes cfg.set_com but ObjectPhysicsEventsCfg declares offset_com.
Ruling 11: fix it — use the declared field `offset_com` (the plan's `set_com` was a guess; consistency with the sibling builder wins). Cost if wrong: none.
Task 6: minor (deferred): one-line docstring on make_object_physics_events_cfg_xyz.
Task 6: fix round 1/5 (1 addressed, 0 open; commits f73cfd9..ea28b7b)
Task 6: complete (commits c63f249..ea28b7b, review clean)
Task 7: implementer DONE_WITH_CONCERNS — test_absolute_ik_holds_pose drifts 0.1356 m in 30 steps with scale confirmed 1.0.
Ruling 12: the drift is gravity sag under the default Franka PD gains (stiffness 80 / damping 4), the reason IsaacLab ships FRANKA_PANDA_HIGH_PD_CFG for task-space control. Use `robolab.robots.franka_high_pd.FrankaCfg` (stiffness 400 / damping 80) as `robot_cfg` in register_test_lift_env; keep the 1 cm hold test. Cost if wrong: a different root cause (IK frame/quaternion) remains and the test still fails — then escalate.
Ruling 13: cube task `contact_object_list = ["rubiks_cube"]` accepted (contact sensors are off in v0).
Task 7: review — 3 Important: (1) no proprio obs group; (2) hold test cannot distinguish IK-holds from nothing-moved; (3) franka_high_pd.FrankaCfg also sets rigid_props.disable_gravity=True, so the drift fix is gravity-off on the robot links, not only gains. Minor: empty tasks/test_lift/__init__.py lacks SPDX.
Ruling 14: (1) proprio observations are NOT required for v0 — the driver reads robot.data (joint_pos, body_pos_w, body_incoming_joint_wrench_b) directly and no policy consumes observations. Add a comment in register_test_lift_env saying so. Cost if wrong: a later learned-policy stage must add the group; trivial.
Ruling 15: (3) accept disable_gravity=True on the robot links as a substrate fact for v0: the object keeps gravity, so the object load still appears in the hand joint wrench; the no-load bias is smaller. Record in the results doc (Task 10) and in the driver's docstring. Cost if wrong: none for the wrench update; the robot's own dynamics are non-physical, which does not affect a quasi-static hold.
Task 7: fix round 1 requested: positive-control IK test (5 cm offset target, converge < 1 cm), SPDX on the empty __init__, proprio comment.
Task 7: fix round 1 — offset-target IK test FAILS (0.497 m) when run after a previous stepped episode + env.reset() in the same env, but converges (9e-6 m) on a fresh env. Matches the known upstream RoboLab bug from the probing study: env.reset() does not restore state under the abs-IK registration.
Ruling 16: v0 uses ONE env.reset() per Isaac process. Tests: order test_absolute_ik_reaches_offset_target first in the module (fresh env, no prior stepping) and keep the hold test after it; add a module comment naming the known issue. Task 8: the driver resets once; --frame-check checks ONE yaw fix per invocation (run it twice with --yaw-fix none / z90); the sweep already runs one process per episode. Cost if wrong: if the bug also bites within an episode (no reset), the second grasp fails to reach its target — the oracle-check and per-arm smoke runs in Task 8 will show it.
Task 7: fix round 2 — reach test PASSES first (9e-6 m); hold test now FAILS third (0.322 m): any stepped episode after a prior stepped episode + reset breaks IK targeting.
Ruling 17: delete test_absolute_ik_holds_pose — the reach test on the fresh env is strictly stronger (hold is the zero-offset case), and a second stepped episode in one env is outside v0's supported usage. Keep test_reset_and_wrench (reset only, no stepping). Cost if wrong: none; coverage is a superset.
Task 7: fix rounds 1-3/5 (4 addressed, 0 open; commits a05e632..003414e)
Task 7: complete (commits ea28b7b..003414e, review clean)
Ruling 18 (peer request, daily-logs-67): the episode log currently stores only the hold-averaged wrench. Before the sweep (Task 10) runs, add two keys — `wrench_trace_h` (HOLD_STEPS,6) and `wrench_bias_trace_h` (HOLD_STEPS,6), raw per-step hand-frame readings — to EPISODE_KEYS/episode_log tests and the driver, as a small Task 8b after Task 8 lands. Reason: the post-v0 adaptation module needs the trace, and re-running 150 Isaac episodes for it would be waste. Cost if wrong: a few KB per episode.
Task 8: implementer DONE (df8a346 graspgen client fix outside allowed files; 0a9795e driver). yaw_fix=z90 measured (controlled A/B: none closes on air, z90 holds+lifts; GraspGenX declares the opening along grasp-frame X, panda_hand closes along Y). Oracle check: sign correct, +x 0.499 kg/0.0 cm, -x 0.498/0.1 cm, +y 0.315/1.7 cm (partial support: 3.10 N of 4.905 N carried). Five arms ran; 5 npz. Grasp success ~50% on the banana.
Ruling 19: accept commit df8a346 (topk_num_grasps=0 returned ZERO grasps; client import failed) — necessary to proceed; reviewed with the task.
Ruling 20: accept the APPROACH_Z_MAX candidate filter — a grasp aimed below the table is invalid for every arm, the filter is applied identically to all arms, and it is a protocol constraint to document (results doc + design §11). Cost if wrong: candidate set is smaller than GraspGenX's raw output; equal for all arms.
Ruling 21: the +y oracle failure is geometry, not sign: a 3 cm y-offset is outside the banana's half-width, so the test-lift leaves the object partly on the table. Sweep offsets become per-object and inside the body: banana x {0.02,0.04}, y 0.015; cube x {0.02,0.03}, y 0.02. Cost if wrong: smaller y effect sizes.
Ruling 22: the RoboLab venv was polluted by `uv pip install yourdfpy` (lxml 7.0.0b1 etc.) during diagnosis — clean it in Task 8b with the report's cleanup command; the server launch needs `--config` (plan text wrong).
Task 8: review (opus) — Critical: belief update runs on a FAILED test-lift (belief seed_0: m_post = -0.849 kg, hold_prob ~1.0, second grasp chosen from it). Important: no IK reach diagnostic for grasp 2 and stale approach filter after the object moves; --seed never reaches the sim (registration hard-codes seed=1) and the scene is deterministic anyway; advance gate uses settle-time gravity direction instead of hold-time. Minors 6-14 listed in the review.
Ruling 21 RESTATED: banana half-extents from the logged prior are [0.054, 0.089, 0.018] m — the +y 3 cm offset is INSIDE the long axis, so the geometry story was wrong. The +y oracle failure is "not a sign error; the object was partly supported (|f| = 3.10 N of 4.905 N), cause unidentified — most likely the same ~50% grasp-success noise". Sign ruling stands on ±x recovery and the top1 hold wrench (4.90 N vs 4.905 N). Sweep offsets stay per-object but the y values are no longer justified by half-width; keep them modest (banana y 0.02, cube y 0.02) and let the data speak.
Ruling 23: gate the belief update on ok1 (b1 = b0 otherwise, hold_prob computed on b0 in that case, log a flag); add ik_err_second and recompute the reach mask against T_obj2 for the second grasp; pass --seed to create_env and registration (scene is deterministic by design in v0 — variation comes from GraspGenX sampling and point subsampling; document); use hold-time gravity in the advance gate and in the oracle-check projection. Minor fixes 8, 10, 12, 13, 14 folded into the same round because they are one-liners in the file being edited. Cost if wrong: none.
Ruling 24 (user approved 2026-09-08): Task 8b scope = (a) wrench trace keys per Ruling 18; (b) per-episode video: `--video` flag on the driver, `RECORD_IMAGE_DATA=True` when set, write `<out>/<obj>/off_XX/<arm>/seed_k.mp4` from the recorded egocentric frames via robolab/core/utils/video_utils.py::VideoWriter; sweep records video for seed 0 of every cell; (c) RoboLab venv cleanup per Ruling 22; (d) plan text fix for the server `--config` flag. Runs after Task 8's fix round closes, before Task 9.
Task 8: fix round 1/5 (5 addressed, 0 open; commits 0a9795e..aa5abd9)
Task 8: minor (deferred): duplicated selector/lift-clear blocks; finger_gap assumes joint order; dead target7=None branch; grasp-2 wrenches discarded; fixed_threshold/oracle/top1 not re-run after the fix.
Ruling 25 (Task 8b): the partial-support guard `‖f_o‖ ≥ 0.5·m_prior·G` is weak when the prior is light (0.77 N threshold vs a 3.1 N partial hold). Strengthen the "real hold" test: rise ≥ 0.9·LIFT_DZ AND object tilt from the settle orientation < 15° AND finger gap > 2 mm. A tilting object at the hold means partial table support or rotation in the fingers, and the rigid-grasp wrench model is invalid either way. Cost if wrong: fewer updates counted; each one that is counted is trustworthy.
Task 8: complete (commits 003414e..aa5abd9, review clean)
Task 8b: review — 1 Important (plan-mandated): the trace round-trip test does not pin the new keys as required (validate_episode ignores extras). Minors: `>` vs `≥` in lift_ok; ik_err2 printed 0.0000 twice (computation unchanged from aa5abd9; watch in the sweep); cosmetic.
Ruling 26: fix in round 1 — add `test_validate_missing_trace_keys` asserting KeyError when either new key is dropped. Cost if wrong: none.
Task 8b: fix round 1/5 (1 addressed, 0 open; commits e524e74..403dccb)
Task 8b: complete (commits aa5abd9..403dccb, review clean)
Task 9: implementer DONE (6560442), 36 passed. Concern: per-object offsets collide in the directory name (x 0.02 and y 0.02 both -> off_02cm) → data loss in the sweep.
Ruling 27: episode directory becomes `off_<axis><mag>cm` with axis = the letter of the largest-|component| of the offset (e.g. off_x02cm, off_y02cm, off_x04cm); the driver builds it, results.py parses `offset_axis` and `offset_cm` from it and adds an `offset_axis` column; the glob becomes `off_*cm`. Fixed in Task 9 fix round 1 (driver edit allowed for this). Cost if wrong: none; the old off_XXcm dirs from Tasks 8/8b are pre-sweep scratch.
Task 9: fix round 1 (Ruling 27 applied; commits 6560442..e33d6fb), 37 passed. Review dispatched.
Ruling 28 (user observation 2026-09-08, "grasp too shallow"): Task 8c before the sweep — diagnose the 2-3 cm grasp-pose shortfall (contact, not IK: free-space reach 1e-5 m). Log along-approach vs lateral shortfall and fingertip height over the table at contact; A/B APPROACH_Z_MAX -0.5 vs -0.85 and --grasp-depth-offset 0 vs +0.01 m on 8 candidates each (4 Isaac processes); pick the setting with the most lifts, apply it as the default for ALL arms, record it in the driver docstring and the results doc. Cost if wrong: a protocol constant chosen on 32 grasps of one object; documented, identical across arms.
Task 9: minor (deferred): to_markdown column presence not pinned by a test; redundant regex match in aggregate; the out_dir change is 3 lines not 1 (accepted).
Task 9: complete (commits 403dccb..e33d6fb, review clean)
Task 8c: running (Ruling 28 diagnostic).
Task 8c: DONE (a46885c). Finding: hand exactly on target (d_along 0, d_lat 0); fingertips stop at the banana crown (tip_z +33.6 mm) → fingertip DEPTH, not IK, not oblique sweep. tip_z ≤ 27 mm gripped 8/8; ≥ 31 mm missed 9/10. Lifts A 0/8, B 0/8, C 1/8, D 3/8; grips 3/1/6/8. Defaults now APPROACH_Z_MAX=-0.85, GRASP_DEPTH_OFFSET=0.01. Harness bug: episode_length_s=60 → time_out at step 900 mid-frame-check (env auto-reset, IK dead) — Task 8's oracle-retry "2-3 cm shortfall" was this artefact.
Ruling 29: the 0.9·LIFT_DZ rise bar (18 mm) rejects real holds that rise 15.1–15.4 mm (object sags in the pads). Lower to 0.7·LIFT_DZ (14 mm); tilt < 15° and gap > 2 mm remain the partial-support guards. Cost if wrong: a partially supported object that rises 14 mm with < 15° tilt passes — the force check still catches gross cases.
Ruling 30: set episode_length_s = 180 in both test-lift task files so no mode can hit mdp.time_out (one frame-check attempt = 246 steps; normal episode ~515). Cost: none.
Task 8c: fix round 1 (Rulings 29-30; commits a46885c..9ac77c8). D re-check: 7/8 grips, 4/8 lifts at 14 mm; two holds rejected by the 15° tilt gate. Review pending (folded into 8d review).
Ruling 31 (user directive 2026-09-08: "never run such a slow sweep with no acceleration"): measured 33 s per grasp of stepping = RTX camera rendering every control step (enable_cameras=True), boot ~20 s. Task 8d before the sweep: (1) no camera unless --video (register without camera_cfg, enable_cameras=False); (2) sweep runs 4 Isaac workers in parallel (xargs -P 4) each under systemd-run --user --scope -p MemoryMax=7G; (3) hard gate: if a no-video episode still takes > 60 s wall (boot + steps), do NOT run the sweep — report. MuJoCo switch deferred (whole-substrate rewrite). Cost if wrong: parallel Isaac processes contend for the GPU; the gate catches it.
Task 8d: DONE (7e781f4). No-camera episode 34 s wall / 26.6 s steps (was 88/68); --video still works. 4 jobs: NWORKERS=4 54 s vs NWORKERS=1 110 s → 2.04x, boot-bound (Isaac boot ~20 s/process), 3.4 GB VRAM + 5.1 GB RSS per worker; RAM caps at 4 workers. Gate PASS. Projected 150 episodes ≈ 40 min. Concerns: obj_rest_z 0.0102 vs 0.0212 in the Task 8 docstring (unverified); OMNI_KIT_ACCEPT_EULA exported by the sweep only.
Ruling 32 (user approved 2026-09-08, "implement vectorized envs"): Task 8e — batched driver `scripts/test_lift_batch.py`. Batch = one (object, offset) cell, num_envs = 5 arms × 5 seeds = 25; lockstep fixed-length phases with per-env target arrays; per-env decisions as numpy masks; advancing envs idle-hold while aborting envs regrasp; GraspGenX called once per seed, shared across the 5 arms of that seed (paired comparison); one batched env.reset() per process; one .npz per env in the existing layout (results.py untouched); no video in batch mode; single-env driver kept for --frame-check/--oracle-check/--video. Sweep becomes 6 cell-jobs, 2 in parallel. Cost if wrong: a 25-env process fails on VRAM/RAM → fall back to the 4-worker single-env sweep from Task 8d (kept).
Task 8c+8d: review clean (approved). Deferred Importants for the final fix wave: (1) registration docstring says 4.6 s/grasp without camera — measured is ~13 s (fix the number); (2) the off_<axis><mag>cm bucket rule is duplicated in awk in the sweep — after 8e the sweep builds one cell dir per job; make the driver print its out_dir and have the sweep read it, or expose a tiny python one-liner both use. Minors: appr_z formula inline ×3; azmax/doff print fragment ×3; .npz count not run-scoped; azmax/doff not persisted in the npz; obj_rest_z zero-offset sanity check.
Task 8c: complete (commits e33d6fb..9ac77c8, review clean)
Task 8d: complete (commits 9ac77c8..7e781f4, review clean)
Task 8e: running (Ruling 32).
Ruling 33 (user request 2026-09-08): after the sweep, run GraspGenX's own end2end Franka demo (`end2end/e2e_grasp_demo.py`, robots/franka_panda.yaml, envs/single_bin_demo.yaml, clutter_pick_and_drop) as a cross-check of grasp depth/convention. Its venv is prepared NOW in the background in a SEPARATE env (`UV_PROJECT_ENVIRONMENT=~/Codes/GraspGenX/.venv-e2e`, `--extra end2end`, `--frozen`), then torch overridden to 2.7.0+cu128 there too, then `setup_end2end_deps.py` with that python. Never touch `.venv` (the serving env). The demo itself runs only after the sweep (GPU). Cost if wrong: cuRobo may not build against cu128/sm_120 — report, do not fight it.
Task 8e: review clean, sweep1 data valid (150/150 files validated by the reviewer). Deferred Important: 5 pure-numpy helpers duplicated across the two drivers (world_approach_z, reachable_candidates, unreachable_after_move, hand_target, tilt_deg) — move to batch.py in the fix wave; batch mode has no video (run one MODE=single cell per object for spot-check videos). Minors: main() 205 lines; zip truncation guard; helpers read module args; sweep grep drops traceback bodies; sweep1 is NOT comparable with single-mode runs (candidate sampling design differs).
Task 8e: complete (commits 7e781f4..96b67f7, review clean)
Sweep1: DONE, 6 cells rc=0, 150 npz. wall per cell 66-78 s with 2 workers.
Sweep1 reading: E1 works when the update fires (banana 1.5→0.12, 3.5→0.40, 1.96→0.04 cm; cube 2.05→0.06; cube x3 3.05→2.56); only 9/30 belief episodes updated. E2: banana at ceiling (0.8-1.0 all arms); cube 0.0-0.6 all arms, oracle ≈ next_best → CoM knowledge does not change the outcome in these cells (geometric grasp failures) → prediction-table row 1, not row 2. Cube second grasps succeed 0/x everywhere (inspect in video).
Ruling 34: fix wave (one subagent): move the 5 duplicated helpers into batch.py (parameterised, both drivers import); LIFT_OK_FRAC 0.7 → 0.6 (12 mm; real holds 15-18 mm, failures ≤ 9 mm; 14 mm sat inside the ±0.3 mm cross-env noise); registration docstring number 4.6 s → ~13 s; assert len(abort_plan) == len(branch_stage_a_schedule()); sweep grep keeps traceback bodies (-A 20). Then sweep2 = the 6 cells + 2 heavy cells (banana x4 @1.5 kg, cube x3 @1.8 kg) as the final data; sweep1 kept as the pilot. Cost if wrong: 6 min of compute.
Ruling 35: spot videos after sweep2: MODE=single, --video, seed 0 of each arm, for banana x4 and cube x3 (10 episodes ≈ 4 min at 4 workers).
e2e prep: READY. `~/Codes/GraspGenX/.venv-e2e` (torch 2.7.0+cu128, curobo editable from ext/curobo, newton 1.0.0). Demo 1 command in task-e2e-prep-report.md. Runs after sweep2 + spot videos (GPU).
Fix wave: DONE (554e92e Part A, 16adb26 Part B; report task-fixwave-report.md). Helpers shared + unit-tested, LIFT_OK_FRAC 0.6, registration 4.6 s -> ~13 s, zip-length assert, sweep keeps traceback bodies + <log>.raw. Tests 64 passed (numpy) + tests/test_test_lift_batch.py PASSED.
Sweep2: DONE, 310 s, 8 cells, 200/200 npz, 0 FAIL. Heavy cells separated as off_x04cm_m1.5kg / off_x03cm_m1.8kg; results.py gained a mass_kg column. wandb run sweep2-final.
Sweep2 reading: heavy banana (1.5 kg) belief E1 3.50 -> 1.89 cm (3/5 updated) vs 3.50 -> 0.15 cm at 0.5 kg -- mass HURTS the estimate here; heavy cube (1.8 kg) belief never updates (0/5) and e2_final_rate 0.0, the worst belief row in the sweep. Open bug candidate: rubiks_cube x02 belief E1 gets worse, 2.05 -> 5.10 cm, from one episode at 17.3 cm (unbounded posterior step on a 7 cm object).
Spot videos (Ruling 35): DONE, 312 s, 10 mp4 under output/test_lift/spot/. Cube second grasp is NOT an IK failure (ik_err 0.0-2.4 mm): grasp 1 tips the cube (tilt1 19-34 deg, obj z 0.021-0.033 vs rest 0.039) and the re-grasp then shuts on air (gap 2.7-12 mm) or gets a marginal hold the lift-clear drops.
Fix wave: DONE (554e92e, 16adb26); 64 numpy tests + batch Isaac test green. Sweep2: 310 s, 8 cells, 200 npz. Heavy cells: banana@1.5kg belief 0.8 / next_best 1.0 / oracle 0.6, E1 3.50→1.89 (3/5); cube@1.8kg belief 0.0 / next_best 0.4 / oracle 0.6, 0/5 updates. Cube failure = grasp 1 tips the cube (tilt 19-34°), regrasp closes on air. BUG CANDIDATE: cube x02 belief posterior 17.3 cm on a 7 cm object (unbounded Kalman step, R_tau tiny) → v1: innovation gate + hull clamp. Spot videos: 10 mp4 under output/test_lift/spot/.
Ruling 36: results doc (Task 10) reports sweep2 as final data, sweep1 as pilot, the row-1 reading (oracle ≈ next_best ⇒ CoM does not decide outcome in these cells), the E1 numbers, the 17.3 cm bug, all substrate facts, and the GraspGenX e2e cross-check outcome. No further sweeps in v0.
Ruling 37 (user observation 2026-09-08: the cube scene is cluttered, a bowl sits next to the cube and the gripper hits it): Task 10b after Task 10 — declare the neighbouring rigid objects of test_plate_banana_rubiks_cube.usda in cube_test_lift_task.py with init_state moved to a far table corner, move the cube to an open spot, verify clearance ≥ 15 cm from the first video frame, re-run the four cube cells (batch mode) into output/test_lift/sweep2 (replace the cube dirs), regenerate the table, and amend the results doc (cube rows + failure-mode paragraph; mark the pre-fix cube numbers as invalid, not provisional). Cost: ~3 min compute.
Ruling 38: v1 to-do list lives in design doc §11.8 (scene-cloud collision filter first); Task 10's results doc §8 must match it.
Task 10 Part A (Ruling 33): e2e demo 1 FAILED then PASSED. First two runs segfaulted (exit 139) in trimesh.collision.in_collision_single <- end2end/clutter_task.py:277, because ext/gripper_descriptions/.../franka_panda/vis_mesh.obj was an unfetched Git-LFS pointer (132 B) -> "Smart-picker fcl: gripper_mesh=0 verts" -> empty FCL BVH. Fixed with `git lfs pull` in ext/gripper_descriptions (gitignored data; only vis_mesh.obj + coll_mesh.obj were stubs; tsdf.npy/points.json, which the model conditions on, were already real -> the study's grasps are unaffected). Re-run SUCCESS: "1/1 objects dropped in bin", object_0 in_bin retries=0, 111 s. 79/200 grasps over the 0.7 threshold, graspmoe 80 total (49 diffusion + 31 OBB) score 0.757..0.868, 36/80 collision-free. Chosen grasp approach = panda_hand +Z [0.0092, -0.0045, -1.0000] (top-down within 0.6 deg) with ZERO depth offset and the z90 tool transform -> the +1 cm depth offset v0 needs is an Isaac-substrate fact, not a GraspGenX convention error. mp4 rendered to end2end/runs/franka_single/franka_single.mp4 (gitignored). GraspGenX git status clean throughout.
Task 10 Part B: docs/studies/2026-09-08-test-lift-v0-results.md written and committed (4cadd9a, 592 lines, NOT pushed). Sweep 2 = final, sweep 1 = pilot (14 mm vs 12 mm bar). All rubiks_cube rows marked INVALID per Ruling 37; verdicts computed on the banana cells only. Predictions: P1 HELD on the light banana x cells (3.50 -> 0.15 cm = 2.1 sigma), marginal at 1.5 kg and not held on y02; P2 NOT HELD (no interval: oracle ~= next_best everywhere); P3 antecedent holds but the consequent does not follow -- row 1, inspect the cell design, not the re-ranker. Section 8 mirrors design 11.8 order per Ruling 38. Rulings 1-38 copied verbatim. Only ONE commit was possible: Part A's artefacts are all outside RoboLab or gitignored, and .superpowers/sdd is gitignored.
Task 10: complete (4cadd9a, results doc 592 lines). e2e demo 1: SUCCESS after `git lfs pull` in ext/gripper_descriptions (vis_mesh.obj was an LFS pointer → trimesh segfault); 1/1 in bin, 79/200 grasps ≥ 0.7, top-down approach, zero depth offset, z90. Predictions (banana only): P1 held on light x cells (3.503 → 0.153 cm), P2 not held (oracle ≈ next_best), P3 n/a (row 1).
Task 10b: dispatched (Ruling 37).
Task 10b: complete (ab800f8 scene fix, 26b7ec5 results). Nearest neighbour 0.35 m after settle (was 0.077 m: the bowl). Cube rows now valid: x02 0.6/0.6/0.6 (E1 2.05→0.92, 3 updated), x03 1.0/1.0/1.0 (E1 no change: posterior moved only in z — suspect a body-frame vs mesh-frame mismatch for the rotated cube (known RoboLab quirk: physics root ≠ mesh prim frame) or hold-time gravity; v1 item), x03@1.8kg 0.6/0.6/0.6 (0 updates), y02 belief 0.4 vs 0.0/0.0. Mean cube e2 0.27 → 0.53 after the scene fix. Oracle ≈ next_best still.
Ruling 39: no more v0 runs. Final whole-branch review next; one fix wave; push to mine; notify peer session.
Final review (opus): FIX THEN PUSH. Blocking: G1 evidence (ledger + task reports) is gitignored → commit under docs/studies/2026-09-08-test-lift-v0-evidence/. Doc fixes: G2 (RoboLab `git lfs pull` prerequisite), G3 ("How to re-run" section: GraspGenX venv/torch override/server command with --config, GRASPGENX_ROOT, EULA, --headless, one reset/process), G4 (sweep commands incl. CELLS=/SEEDS= overrides), G5 (plan header: stale sweep script), G6 (untraceable numbers: pre-fix cube "before" column → wandb ibeewkgx only; 17.3 cm episode from the evidence dir; fact 9 bias source; fact 12 dangling ref; g_o not logged; protocol constants not in npz — say which sweep used which LIFT_OK_FRAC). R1 (5.59 cm posterior norm is from the prim origin; from the centroid it is 2.52 cm, inside the body — correct §4.6/§8.2), R2 (cube y02 P1 rests on the §4.6 episode — cross-reference), R3 (rest pose changes with the offset: per-cell obj_rest_z table; §4.7 mechanism is x03-only; driver docstring rest z = 0.0212 is wrong), R4 (fixed-gravity E1 metric: state the effect is < 0.1 mm here; log g_o in v1), R5 (do not attribute cube x03 to the gravity mismatch; promote the "torque's informative direction maps onto object z" reading; x02 vs x03 differ in rest pose), R6 (frame hypothesis REFUTED — record it), R7 (hand-origin moment reference assumption), R8 (§1 over-states E1: 2 held / 2 marginal / 4 not held of 8), R9 (batch physics shares one scene; filter uses arm-0 pose per seed). Code: Q1 (move select_first/select_second into batch.py, both drivers import), Q2 (promote R_F, R_TAU and the supported gate into batch.py; repoint §6), Q3 (assert finger joint names), Q8. Ledger closes: Ruling 6, T3 depth comment, T8 arms re-run, 8c/8d docstring, appr_z, T8e items, T9 out_dir.
Final fix wave: DONE (commits 6d02196 code, docs+evidence next). Code: Q1 select_first/select_second moved into analysis/test_lift/batch.py, fully parameterised (arm, grasps, confs, belief, m_true, c_true, g_hat, params, rng, exclude), both drivers import them; Q2 R_F, R_TAU and update_allowed(ok, f_o, m_prior) promoted to batch.py, both drivers import them; Q3 assert_finger_joints(joint_names) called in both drivers' robot constructors; Q8 the hard-coded banana rest z 0.0212 in scripts/test_lift_episode.py's docstring replaced by "measured per cell, printed on the [table] line" plus the sweep-2 per-cell values. Tests 73 passed (was 64): 9 new pinning the arm->selector map, exclude handling, the unknown-arm error, the update gate boundary (including the Task 8 partial-support case), R_F/R_TAU, and the finger-joint assert. Evidence bundle committed under docs/studies/2026-09-08-test-lift-v0-evidence/ (ledger + every task-*-report.md + scene_check_frame0.png); results doc §2/§4.6/header repointed to it; new §3.6 "How to re-run" and §3.7 "The exact sweep commands"; plan doc carries a status header saying the results doc is authoritative.
Ledger closes (final fix wave):
- Ruling 6 parked item: CLOSED, no action. Not a defect (exact Bayes); the spec §11.2 wording correction belongs to the design doc, not to this branch.
- Task 3 minor "FRANKA_PANDA_DEPTH lacks a provisional comment": CLOSED. Task 5 replaced the value with the real one and rerank.py:15-19 now carries the full source note (which GraspGenX API, and why not the legacy one), so there is nothing provisional left to flag.
- Task 8 minor "fixed_threshold/oracle/top1 not re-run after the fix": CLOSED. Sweep 1 (150 episodes) and sweep 2 (200 episodes) both ran all five arms after every fix; the Task 8 single-arm runs are pre-sweep scratch.
- Task 8c/8d minor "registration docstring says 4.6 s/grasp": CLOSED in the Ruling 34 fix wave (554e92e), corrected to ~13 s.
- Task 8c/8d minor "appr_z formula inline x3": CLOSED. world_approach_z lives in batch.py (554e92e) and the two remaining inline copies in scripts/test_lift_episode.py (attempt_report, --frame-check) now call it (6d02196).
- Task 8e Important "5 pure-numpy helpers duplicated across the drivers": CLOSED in 554e92e; the final wave closed the last two duplicated blocks (the selector dispatch and the update gate).
- Task 8e minor "zip truncation guard": CLOSED in 554e92e (assert len(abort_plan) == len(branch_stage_a_schedule())).
- Task 8e minor "helpers read module args": CLOSED. Every shared helper, and now both selectors, take each value they use as an argument; nothing in batch.py reads a driver's argparse namespace.
- Task 8e minor "sweep grep drops traceback bodies": CLOSED in 554e92e (grep -A 20 plus a <log>.raw copy of the full stdout).
- Task 8e minor "sweep1 is NOT comparable with single-mode runs": CLOSED as documented, results doc §4.8.
- Task 9 minor "the out_dir change is 3 lines not 1": CLOSED, accepted as written.
Still open (carried to v1, none blocking): Task 1 kappa/alpha test; Task 2 sample() clip comment, ConvexHull guard, missing docstrings; Task 3 1e-6 clip untested; Task 4 HAND_YAW_FIX mutable module state, no standalone subtract_bias test, bare KeyError; Task 5 available() catches bare Exception, validate_episode checks keys not shapes; Task 6 one-line docstring; Task 8 dead target7=None branch, grasp-2 wrenches discarded; Task 8c/8d azmax/doff print fragment x3, .npz count not run-scoped, azmax/doff not persisted in the npz, obj_rest_z zero-offset sanity check; Task 8e main() is 205 lines, no video in batch mode; Task 9 to_markdown column presence not pinned by a test, redundant regex match in aggregate; the off_<axis><mag>cm bucket rule is still duplicated in awk in the sweep script.
Final fix wave: re-review clean (all G/R/Q addressed, 73 tests). Pushing.
