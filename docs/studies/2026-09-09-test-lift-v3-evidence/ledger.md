# SDD ledger — plan: docs/studies/2026-09-09-test-lift-v3-plan.md
Spec: daily-logs designs §14 (priors) + graded-commitment design §4/§11.2/§11.8. Workspace: main checkout, branch study/test-lift-belief-rerank.

## Preflight scan
| pair / task | produces vs consumes | finding |
|---|---|---|
| T1↔T2 | hold_verdict->(held,swung); update_allowed(held,...); LIFT_STEPS; swing.* fns; belief.update_from_swing; prior_from_points(mass_prior=False) | consistent; T1 must grep every update_allowed call site (dataset.py passes lift_ok -> still valid) |
| T2 self | run_batched_grasp records the lift trace via wrench_window(…, LIFT_STEPS) — check wrench_window returns (mean, trace) with the requested length | verify in T2 |
| T2↔T4 | new npz keys held1/swung1/tilt_wrench1/d_along1/lift_trace_h; results.py extended with e1_along_cm, n_swung | consistent |
| T3↔T4 | candidates dumps under output/test_lift/v3/candidates/<obj>.npz; OBJECT_MASS_KG entries | consistent |
| T1 physics | pendulum: tau_axis = m G d_perp; d_along = d_perp / tan(phi); sign convention 'below the tips = along +g' fixed by the test | consistent with test_pendulum_geometry |
Clean.

## Tasks
Task 1: dispatched (BASE 9d1a3a6). Task 3: dispatched in parallel — new task files + asset checks only; it must NOT edit batch.py until Task 1 lands (OBJECT_MASS_KG entries deferred to a follow-up commit after Task 1).
Task 1: implementer DONE commit 6f6a73f (174 tests). Concern: brief prose said tau_axis = m G d_perp; the cross product r x F gives tau.axis = -m G d_perp, so d_perp = -(tau.axis)/|f|; the pendulum test binds and is physically right (Ruling 1: test binds; the brief's prose was loose). Two old test_batch assertions pinned the v0 "belief first pick" contract and were updated to the §14 decision.
NOTE: Task 1 committed batch.py changes while Task 3's Isaac asset checks were running (my parallel dispatch). Impact: the asset check uses --label-all (no belief arm) and real_hold is semantically unchanged, so the check outcome is unaffected. Ruling 2: accept; do not repeat — Task 2 (driver edit) waits for Task 3's Isaac runs to end.
Task 3: implementer DONE commit 306a818. Asset check: mustard PASS (reach 97%, coa 16%), measuring_cup PASS (91%, 3%), wood_hammer FAIL (71%, coa 100%), cordless_drill FAIL (66% reach), spam_can FAIL (69% reach).
Ruling 3 (v3): second asset criterion, applied uniformly to all nine objects: the mesh bottom transformed by the root pose must sit within 1 cm of the table (`z_table` <= 0.013). Measured z_table: hammer .006, drill .049, spam .003, mustard .003, cup .050, cracker .002, banana/cube/mug ≈ .003. The drill and the measuring_cup FAIL this criterion (mesh frame 5 cm off the physics root), so the cup's PASS is void. v3 objects: banana, rubiks_cube, mug, mustard. Cost if wrong: one object fewer.
Ruling 4 (v3): Ruling 14 of v1 stated the cracker_box cause as a physics-root/mesh offset. Measured now: centroid vs authored CoM 0.5 cm, z_table 0.002 — frames are consistent. The actual signature is tips 0.7 cm ABOVE the top face of the upright 21 cm box (tip_z 220 mm vs top 213 mm): a grasp-height problem on tall objects, cause not yet established. The v1 results doc's caveat wording is wrong on the cause and must be corrected in the v3 results doc (Task 4), not silently.
Ruling 5 (v3): wood_hammer — frames consistent, authored CoM 8.9 cm from the centroid (genuinely head-heavy, the object the user asked for), but tips land 124 mm above the table for a 31 mm-thick object (median over 12 reached envs). Unexplained; excluded from v3 by the pre-registered check; a 20-minute video diagnostic is the first v4 item. Cost if wrong: the most interesting object waits one round.
Task 1: minor (deferred): no zero-force guard in along_gravity_from_swing; unused g_hat_o/p_tip_o params kept for interface parity. Task 1: complete (commits 9d1a3a6..6f6a73f, review clean).
Task 3: minor (deferred): report count phrasing; episode_length_s boilerplate duplicated. Reviewer note: Task 3's PASS list is superseded by Ruling 3 (cup voided) — Task 4's doc must say so. Task 3: complete (commits 6f6a73f..306a818, review clean).
Task 2: implementer DONE commits cfd19f0 (OBJECT_MASS_KG) + 73b0f11 (driver). Mass without prior: m_post 0.4001 vs 0.400 kg. Swing: the mug's held+swung rotation (one grasp, cand 2, 6 repeats) is 22.8 deg total: about finger axis x 2 deg, pad-plane axis y -19, approach z -13. The 2 cm smoke's 'swung' episodes never left the table (held=False) — levered on the table edge, rotation spread over all axes. Wrench-only tilt estimate does not match GT (33-68 vs 20 deg). Implementer added plausibility gates (|d_along| <= half-extent; only when held) — accepted.
Ruling 6 (v3): the pendulum model's assumption (rotation about the finger axis) is checkable from the GT rotation in sim. The swing update is applied ONLY when |rv.x_axis| >= 0.8 |rv| (rotation predominantly about the finger axis); otherwise the episode logs `[swing-skip]` and no CoM update from the swing. New key `swing_axis_frac1` = |rv.x|/|rv|. Task 4 reports the fraction of held+swung episodes that satisfy it, per object — this is the first empirical test of the swing model, and it may come back near zero. The wrench-only tilt (`tilt_wrench1`) stays logged as a side measurement but is reported as failed unless Task 4's statistics say otherwise. Cost if wrong: the swing evidence is used in fewer episodes.
Task 2: fix round 1 dispatched (resume implementer) for Ruling 6. FIX_BASE 73b0f11.
Task 2: fix round 1 landed dd625bb (175 tests); frac gate BEFORE the MIN_SWING_DEG nan (else the assumption failure is invisible) — accepted. Mug swings are off-axis (frac 0.05–0.07): the swing update never fires there. Review dispatched.
Task 2: review — Approved with follow-ups: Important 1 results.py n_updated wrong under nan prior; Important 2 single-env driver diverged (ruled: keep v0 behaviour there, document). Minors: log swing_axis_frac1 always + phi1; stale comments. Fix round 2 dispatched (resume). tilt_wrench1 remains an unvalidated diagnostic (Task 1's tilt_from_wrench_trace fails Step 4's ±10° criterion) — Ruling 7: reported as failed side measurement in Task 4 unless the sweep says otherwise.
Task 2: fix round 2/5 re-review — all addressed (commits dd625bb..99fe186, 177 tests). Task 2: complete (commits 306a818..99fe186, review clean).
Task 4: implementer DONE commits 2b70854, d90de75, b174e38 (183 tests). 400+80 episodes, 0 FAIL. Pooled E2: oracle .562 top1 .500 next_best .475 belief .438 fixed_thr .350; belief v0-control .500. Swing updates fired: 0/400 (no held+swung episode at 2 cm). [no-mass] fallback on 50/80 belief episodes. Heavy-cell mass under-read 0.174 kg unexplained. Combined Task-4 + whole-v3 final review dispatched.
FINAL REVIEW v3 (9d1a3a6..b174e38): Approve with follow-ups — Important 1 results.swing_update_fired omits the driver's belief-arm and half-extent conditions (no v3 effect, v4 hazard); Important 2 doc says 'four of five updated cells' — six cells, cube x02 12.9 g outlier unnamed. Minors 3-12. Fix wave dispatched.
Fix wave re-review: 12/12 addressed. Task 4: complete. v3 BRANCH CLEAN at e6e7fe7 (185 tests).
